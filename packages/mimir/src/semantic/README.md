# semantic/

Tier 2 semantic search module. Vectorizes experiences and insights via Ollama embedding API, then performs cosine similarity search.
Automatically skipped when no endpoint is configured — Tier 1 (SQL+FTS5) operates independently.
