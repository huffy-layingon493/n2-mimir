// Ark rule converter — insight → Ark .n2 rule generation (Phase 4 skeleton)
import type { Insight } from '../types.js';

/**
 * Convert graduated insight to Ark .n2 rule format.
 * SKELETON — full implementation in Phase 4.
 */
export function toArkRule(_insight: Insight): string | null {
  // Phase 4: generate Ark .n2 rule from graduated insight
  // Example output:
  // @rule { pattern: "배포판 접근", action: "BLOCK", message: "배포판은 건들지 마세요" }
  return null;
}
