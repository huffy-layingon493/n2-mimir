// Clotho workflow converter — insight → Clotho workflow generation (Phase 4 skeleton)
import type { Insight } from '../types.js';

/**
 * Convert graduated insight to Clotho workflow format.
 * SKELETON — full implementation in Phase 4.
 */
export function toClothoWorkflow(_insight: Insight): string | null {
  // Phase 4: generate Clotho workflow from graduated insight
  // Example output:
  // @workflow coding_review { steps: [read_design, verify_structure, code, verify] }
  return null;
}
