# store/

Data persistence layer for Mimir. Built on SQLite (better-sqlite3).

- `schema.ts` — SQL table and index definitions
- `database.ts` — Experience/Insight CRUD, FTS5 search, voting, effect tracking
- `index.ts` — barrel export
